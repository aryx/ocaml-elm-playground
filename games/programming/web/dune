; claude: the same games as ../dune, in the browser
(copy_files ../*.ml)

(executables
  (names TinyCoreWar)
  (modules TinyCoreWar)
  (libraries
    elm_playground
    elm_playground_web ; implem of virtual elm_playground
  )
  (modes js)
  (link_flags -no-check-prims)
)

; the .html pages next to the .bc.js
(rule
  (alias default)
  (deps (glob_files *.html) (alias all))
  (action (progn)))
