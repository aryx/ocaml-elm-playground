(* The editors: Gap_buffer's worked example and its old versions kept
 * whole, then TinyEmacs played key by key -- typing and undo as runs,
 * killing and yanking, Lisp evaluated in *scratch*, M-x completing,
 * C-h k reading its keymap, .emacs's own command, a file visited and
 * saved, incremental search, C-u -- and TinyVi the same way *)
val tests : Testo.t list
