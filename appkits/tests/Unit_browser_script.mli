(* Browser_script, on fake pages: the notes' worked examples (a text
 * changed; a hundred items appended, one change to lay out), innerHTML
 * written and read, selectors, style and class, an element's one host
 * object, the tree edited (moved, inserted, removed, never inside
 * itself), the title, Netscape's attributes kept through the copy, and
 * the scripts' errors in the console, the next script still run; the
 * events: a click bubbling, stopped, prevented (onclick="...; return
 * false" too), delegated to a table; keys and a field's input; timers
 * on the page's clock; alert and DOMContentLoaded *)
val tests : Testo.t list
