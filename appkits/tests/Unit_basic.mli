(* BASIC: Basic_parse's worked example, the cruncher (FORI=1TO9, and
 * SCORE read as SC OR E), precedence and the refusals; programs run on
 * answers in both dialects -- Integer's 16 bits and columns of 8,
 * Applesoft's floating point printed its way and its dice -- strings
 * and their functions, FOR/NEXT, arrays, DATA, DEF FN, ON GOTO, IF
 * guarding its line, INPUT's fields; the session's editing and FP;
 * 10 GOTO 10 stopped by Control-C on a machine; and the differential
 * test: Basic_session's Guess listing and Tty_guess playing the same
 * games, PLAY AGAIN included *)
val tests : Testo.t list
