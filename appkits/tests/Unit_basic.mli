(* BASIC: Basic_parse's worked example and its refusals, programs run
 * on answers (16-bit wrapping, GOSUB, a computed GOTO, PRINT's
 * columns, the errors), the session's editing, 10 GOTO 10 stopped by
 * Control-C on a machine, and the differential test: Basic_session's
 * Guess listing and Tty_guess playing the same games *)
val tests : Testo.t list
