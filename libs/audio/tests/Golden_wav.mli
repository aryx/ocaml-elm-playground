(* Golden WAVs: the sounds of audio/ written to actual/*.wav and compared
 * sample by sample (as 16-bit integers) with golden/*.wav, like the
 * golden frames of tests/2d/ *)

val tests : Testo.t list
