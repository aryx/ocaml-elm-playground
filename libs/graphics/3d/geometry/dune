; claude: what all the 3D backends share (the 3D Playground's web one,
; the software rasterizer, OpenGL): vectors (Vec3), the camera (Camera)
; and its matrices (Mat4), and the lighting formula (Lighting). Depends
; on nothing. A private library (see graphics/core/dune), installed as
; part of elm_playground_3d, the package all the 3D backends depend on.
(library
 (name graphics_3d_geometry)
 (package elm_playground_3d)
 (wrapped false)
)
