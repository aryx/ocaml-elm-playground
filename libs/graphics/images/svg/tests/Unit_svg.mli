(* Svg: the worked example (a red square in a 4 by 4 picture), the
 * viewBox's scaling, the path grammar (relative commands, numbers run
 * together), an arc's circle, the evenodd rule's hole, currentColor,
 * reading XML, recognizing an SVG file, its size *)
val tests : Testo.t list
