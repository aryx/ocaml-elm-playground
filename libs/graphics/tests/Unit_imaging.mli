(* Imaging (libs/graphics/imaging): each .mli's worked example -- levels
 * and gamma, a histogram, red turned green, a blur and a sharpen, the
 * Gaussian's kernel, Sobel on an edge, a speck removed by the median,
 * a bilinear ramp, a rectangle's mask, a mix through a mask, a dab and
 * a stroke's spacing, a gradient -- and TinyPhotoshop's photographs
 * decoded by our own JPEG reader *)
val tests : Testo.t list
