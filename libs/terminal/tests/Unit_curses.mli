(* Curses: the .mli's worked example (CAT HAT to BAT HOT, 12 bytes),
 * nothing sent for nothing changed, the cursor shown and hidden, and
 * the property that matters: a VT100 (Vt) fed a screen's redraw then
 * the refresh to another shows the other, cell for cell, on random
 * screens *)
val tests : Testo.t list
