(* Talk: a conversation run on answers (the .mli's name example),
 * the same seed giving the same numbers, and the machine: a line typed
 * with a typo fixed, a key in raw mode, Control-C ending the program,
 * the baud rate rationing the printing *)
val tests : Testo.t list
