(* Mail and Mbox: the .mlis' worked examples -- a message's fields,
 * folding, addresses and dates; a mailbox with its >From lines --
 * each read and written back as the same bytes *)
val tests : Testo.t list
