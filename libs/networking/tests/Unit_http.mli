(* Http: the request's bytes, the status line, the chunked
 * worked example, and each of the four ways a body ends; the server's
 * side, a request whole or not yet (the .mli's examples) and a
 * response written then read back *)
val tests : Testo.t list
