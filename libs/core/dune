(include_subdirs unqualified)

; claude: Cmd and Sub are here too, as in Elm, whose core has them
; (Platform.Cmd, Platform.Sub). The folders are folders only, the
; library the same whichever one a module sits in: time/ (the calendar
; and the wall clock: Civil, Clock, Julian, Recur, Ics); tests/ has a
; stanza of its own, so it says (include_subdirs no).
(library
 (name elm_core)
 (public_name elm_playground.core)
 (wrapped false)
 (libraries
   ; commons; was for Set_.ml but now included here
   caps ; claude: Cmd.Http_get carries the program's Cap.network
 )
 ; (preprocess (pps profiling.ppx))
)
