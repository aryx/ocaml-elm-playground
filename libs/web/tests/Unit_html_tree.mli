(* Html_tree: the notes' worked example (four repairs), then each rule
 * of the .mli -- end tags left out, the search stopped by nested lists
 * and cells, stray and misnested end tags, void elements, the head and
 * the body, the newline after <pre> *)
val tests : Testo.t list
