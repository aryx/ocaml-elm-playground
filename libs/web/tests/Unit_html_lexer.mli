(* Html_lexer: the notes' worked example, and each of the .mli's
 * "what makes it HTML's" cases: the '<' that is text, the three
 * quotes, entities in values, comments and doctypes, self-closing
 * tags, RAWTEXT and RCDATA, a tag cut off, line ends; Netscape's
 * names and values marked (Dtd.origin) *)
val tests : Testo.t list
