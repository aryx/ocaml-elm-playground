(* Cascade, Css_values and Computed: the notes' worked examples (the
 * cascade's order, !important over specificity; var() and a nearer
 * definition; calc(100% - 2em)), media queries, the user agent's sheet,
 * inheritance and its keywords, the shorthands, the colours *)
val tests : Testo.t list
