(* Charset: the .mli's "café" in both encodings and the mojibake
 * between them, Word's quotes labelled Latin-1, the labels, the
 * header, the <meta> prescan, the guess, and malformed UTF-8 *)
val tests : Testo.t list
