(* Html_layout, with a character as wide as its size: the .mli's
 * worked example to the tenth, a baseline shared by two sizes, spaces,
 * looks inherited, indents and rules, lines broken only where the page
 * says, alignment, what is not shown; Netscape's extensions ignored by
 * Mosaic's layout and honoured by Netscape's: fonts, rules, and the
 * floats' worked example *)
val tests : Testo.t list
