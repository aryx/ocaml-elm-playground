(* Box_layout: the worked examples (auto margins centring, the notes'
 * 277), box-sizing, margins collapsing (through a parent, between
 * siblings, an empty block's), floats and clear, an inline-block
 * shrunk to fit, relative and absolute positioning, a list's markers,
 * a table's columns, the attributes' hints, a picture's max-width *)
val tests : Testo.t list
