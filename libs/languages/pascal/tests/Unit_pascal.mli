(* Pascal: the lexer's worked example, the code of x := a + b * 2 as
 * Pcode.mli prints it, every program of the disk run to its known
 * output, static links in the code of nested procedures, the
 * compiler's first error with its line and column, the run-time
 * errors, 16-bit arithmetic, and Pascal's reading of lines *)
val tests : Testo.t list
