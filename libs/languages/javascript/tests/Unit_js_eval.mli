(* Js_eval and Js_builtins: the notes' worked examples (the closures of
 * a counter, a let per iteration, the "Wat" coercions), this in methods
 * and arrows, the errors named and on their line, try and throw, the
 * limits (recursion, a loop that never ends), and the built-ins *)
val tests : Testo.t list
