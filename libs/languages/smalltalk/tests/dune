; claude: unit tests for the Smalltalk-80 library, run by 'make test'
; (dune runtest): the worked examples of notes_smalltalk.md and of the
; .mlis -- the grammar's precedence, the metaclass knot, a method's
; bytes, a non-local return, 100 factorial, a debugger session
(test
 (name Test)
 (libraries alcotest testo smalltalk)
)
