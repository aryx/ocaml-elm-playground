(* Smalltalk-80: the lexer's traps, the parser's precedence, the
 * Blue Book's compiled Rectangle>>center byte for byte, the inlined
 * control structures, the metaclass knot, become:, the collector, the
 * interpreter (sends, primitives failing into Smalltalk, non-local
 * return, doesNotUnderstand:), the kernel (100 factorial, fractions,
 * collections, streams), the debugger's stepping and restarting, and
 * the image saved and loaded *)
val tests : Testo.t list
