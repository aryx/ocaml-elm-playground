(* Our_mail: every built-in mailbox read and written back as the same
 * bytes; each In message's lesson there (the thread's References, the
 * forged envelope, the digest, the attachment, the accents, the
 * >From line); the nicknames' vCards *)
val tests : Testo.t list
