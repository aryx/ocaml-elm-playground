; claude: the same apps against the software rasterizer, for the golden
; frames (see examples/software/dune): ../Foo.ml reused as-is by a
; build-time copy, not a committed duplicate.
(copy_files ../*.ml)

(executables
  (names TinyClock TinyCalendar)
  (modules TinyClock TinyCalendar)
  (libraries
    caps ; the File menu's authority to save, Export's and Import's
    gui ; Widget, Layout, Theme
    appkit_file_menu ; File_menu
    appkit_pim ; Ics
    elm_playground
    elm_playground_software ; implem of virtual elm_playground
  )
)
