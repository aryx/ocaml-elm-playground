; claude: the classic building blocks of a sound (notes_audio.md
; sections 3 to 8): the oscillators (Oscillator, band-limited or not),
; noise (Noise), FM (Fm), a plucked string (Pluck), the envelope
; (Envelope), the filters (Filter). Each a formula or a small state
; over samples, used by the engine above (Synth renders with them) and
; by instruments/ and effects/. A private library, installed as part
; of elm_playground.
(library
 (name audio_synthesis)
 (package elm_playground)
 (wrapped false)
 (libraries audio_signal)
)
